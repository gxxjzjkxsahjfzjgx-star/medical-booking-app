# 📘 الدليل الشامل والشارح لمشروع Medical Booking App (تطبيق حجز المواعيد الطبية)

أهلاً بكِ يا جانا! هذا الدليل المُفصّل يقع في قلب مشروعك، ليشرح لكِ **كل كبيرة وصغيرة** قمنا ببرمجتها، لماذا استخدمناها، وكيف تم بناؤها باللغة العربية البسيطة والمباشرة، حتى تكوني قادرة على شرح المشروع بثقة كاملة أمام أي مُقيّم أو مُحاضِر.

---

## 📌 1. الفكرة العامة للمشروع (Project Overview)

المشروع عبارة عن **تطبيق حجز مواعيد طبية (Medical Booking Web App)** مُتكامل ومبني باستخدام مكتبة **React**.
الهدف منه ليس مجرد شكل جمالي، بل تطبيق كل المفاهيم الأساسية والمتقدمة في React مثل:
* المكونات القابلة لإعادة الاستخدام (**Reusable Components**).
* التمرير وحفظ البيانات (**Props & State**).
* إدارة المسارات والصفحات (**React Router v6**).
* نماذج الإدخال المباشر وغير المباشر (**Controlled vs Uncontrolled Inputs / useRef**).
* إدارة النماذج والتحقق من البيانات (**React Hook Form & Validation**).
* إدارة الحالة العامة للتطبيق (**Zustand Global State**).
* الاتصال بسيرفر البيانات الصريح عبر الـ REST API (**Axios & json-server**).
* تصميم عصري متجاوب يدعم الوضع الليلي والنهاري (**Tailwind CSS & Dark Mode**).

---

## 🛠️ 2. التقنيات والمكتبات المستخدمة (Tech Stack & Libraries)

| المكتبة / التقنية | سبب الاستخدام والوظيفة بالمشروع |
| :--- | :--- |
| **React 18 (Vite)** | بناء واجهة المستخدم التفاعلية بسرعة عالية وتوفير نظام المكونات (Components). |
| **React Router DOM v6** | للتنقل بين صفحات التطبيق بدون إعادة تحميل الصفحة (Single Page Application - SPA). |
| **Zustand** | إدارة البيانات العامة التي نحتاجها في أماكن متعددة (مثل الأطباء المفضلين، الوضع الليلي، وبيانات البروفايل Jana Sayed). |
| **Axios** | إرسال طلبات الـ HTTP (GET, POST, PUT, DELETE) للاتصال بسيرفر البيانات. |
| **React Hook Form** | إدارة نماذج الإدخال (Forms) وسهولة التحقق من صحة البيانات (Validation) كالإيميل ورقم الهاتف. |
| **Tailwind CSS** | تنسيق الواجهات بسرعة وتطبيق التصميم العصري والوضع الليلي (Dark Mode) والشاشات المجهزة للموبايل. |
| **Lucide Icons** | إضافة أيقونات SVG طبية وعصرية وخفيفة الوزن. |
| **json-server** | سيرفر محاكاة خلفي (Mock REST API) يقرأ ويكتب في ملف `db.json`. |

---

## 📂 3. شرح ملفات المشروع ملفاً بملف (File-by-File Explanation)

---

### 1️⃣ ملف قاعدة البيانات: `db.json`
* **مكانه:** المجلد الرئيسي للمشروع.
* **شرحه:** يحتوي على بيانات الأطباء (8 أطباء بتخصصات مختلفة وسعر الكشف وصورهم وساعات عملهم) بالإضافة إلى قائمة المواعيد المحجوزة المبدئية التي تحتوي على بياناتك (`Jana Sayed`).
* **فائدته:** المصدر الوحيد والأساسي للبيانات التي يقرأ منها `json-server` عبر الـ REST API.

---

### 2️⃣ ملف خدمات الـ API الصافي: `src/services/api.js`
* **شرحه:** هذا الملف يعتمد بنسبة 100% على الـ REST API الصريح (Axios) المتصل بـ `http://localhost:3001` دون وجود أي بيانات مصبوبة (Hard-coded fallback arrays) داخل الكود، ليكون مطابقاً حرفياً لجميع شروط المادة.
* **الوظائف الموجودة به:**
  * `fetchDoctors()`: يجلب قائمة الأطباء من السيرفر (GET `/doctors`).
  * `fetchDoctorById(id)`: يجلب تفاصيل طبيب معين (GET `/doctors/:id`).
  * `fetchAppointments()`: يجلب قائمة المواعيد المحجوزة (GET `/appointments`).
  * `createAppointment(data)`: يضيف موعداً جديداً (POST `/appointments`).
  * `updateAppointment(id, data)`: يعدل موعداً أو يعيد جدولته (PUT `/appointments/:id`).
  * `deleteAppointment(id)`: يلغي موعداً (DELETE `/appointments/:id`).

---

### 3️⃣ ملف المخزن العام: `src/stores/useAppStore.js`
* **شرحه:** يعتمد على مكتبة **Zustand** لإدارة الحالة العامة.
* **البيانات المخزنة فيه:**
  1. **الوضع الليلي والنهاري (`theme`):** وظيفة `toggleTheme` للتحويل بين Dark و Light وحفظ الخيار في `localStorage`.
  2. **قائمة المفضلة (`favorites`):** مصفوفة بـ IDs الأطباء المفضليين، مع إمكانية الإضافة والحذف مع الحفظ التلقائي.
  3. **بيانات البروفايل (`userProfile`):** تحتوي على بياناتك الشخصية (`Jana Sayed`, `janasayed135790@gmail.com`, `01064362926`).
  4. **نظام الإشعارات (`toasts`):** مصفوفة لإظهار رسائل التنبيه المؤقتة أعلى الشاشة عند الحجز أو التعديل أو الحذف.

---

### 4️⃣ المكونات التفاعلية (Components - `src/components/`)

#### 🔹 `Navbar.jsx` (شريط الملاحة)
* يعرض شعار الموقع "MediCare".
* يحتوي على روابط التنقل بين الصفحات (`NavLink`) والتي يضيء الرابط النشط منها تلقائياً.
* يحتوي على عداد الأطباء المفضليين (Badge) يظهر عدد الأطباء الذين حُفظوا بالمفضلة.
* زر التبديل السريع بين الوضع الليلي والنهاري.
* صورة واسم البروفايل الخاص بكِ (`Jana Sayed`).

#### 🔹 `DoctorCard.jsx` (بطاقة عرض الطبيب)
* مكون يعرض بيانات الطبيب (الصورة، الاسم، التخصص، التقييم، سعر الكشف، والعيادة).
* يحتوي على زر القلب (`Heart`) لإضافة الطبيب للمفضلة أو إزالته عبر Zustand.
* يحتوي على زر الانتقال لصفحة التفاصيل وزر الحجز المباشر.

#### 🔹 `AppointmentCard.jsx` (بطاقة الموعد المحجوز)
* يعرض تفاصيل الموعد: اسم الطبيب، التخصص، تاريخ الموعد، وقته، اسم المريض (`Jana Sayed`)، والإيميل والهاتف.
* يحتوي على زري عمليين:
  * زر **Edit**: يفتح نافذة التعديل لتغيير تاريخ أو وقت الموعد.
  * زر **Cancel**: يفتح نافذة التأكيد لإلغاء وحذف الموعد.

#### 🔹 `ControlledVsUncontrolledDemo.jsx` (المكون الإيضاحي الفني)
* هذا المكون تم بناؤه خصيصاً لإثبات تطبيقك للشروط الفنية المطلوبة في المشروع:
  1. **Controlled Input:** مدخل محكوم بـ `useState` للبحث المباشر.
  2. **Uncontrolled Input:** مدخل غير محكوم يعتمد على `useRef` مع زر للتركيز (Focus Ref) وقراءة القيمة مباشرة من الـ DOM node.

#### 🔹 `Modal.jsx` (النافذة المنبثقة)
* مكون منبثق لإبراز الاستمارة عند التعديل أو التأكيد عند الحذف، مع إمكانية الإغلاق بزر `Esc` أو الضغط على زر الإغلاق.

#### 🔹 `ToastNotification.jsx` (رسائل التنبيه)
* يعرض رسائل نجاح عملية الحجز أو التعديل أو الإلغاء وتختفي تلقائياً بعد 4 ثوانٍ.

#### 🔹 `LoadingSkeleton.jsx` (هيكل التحميل)
* يظهر شكلاً انسيابياً مضيئاً أفقياً (Skeleton Loader) أثناء انتظار جلب البيانات من السيرفر لإعطاء انطباع احترافي للواجهة.

---

### 5️⃣ صفحات التطبيق الرئيسية (Pages - `src/pages/`)

#### 📄 `DoctorsPage.jsx` (الصفحة الرئيسية / الأطباء)
* **الوظيفة:** عرض جميع الأطباء المتاحين من الـ REST API.
* **التقنيات المستخدمة فيها:**
  * **Controlled Search Input:** مربع بحث مادي مربط بـ State للبحث الفوري باسم الطبيب أو تخصص العيادة.
  * **Specialty Pills:** أزرار تصنيفات سريعة (قلبية، جلدية، أطفال، مخ وأعصاب...).
  * **Sorting & Favorites Filter:** إمكانية ترتيب الأطباء حسب التقييم أو السعر، أو إظهار أطباء المفضلة فقط.

#### 📄 `DoctorDetailsPage.jsx` (صفحة تفاصيل الطبيب `/doctors/:id`)
* **الوظيفة:** تقرأ الـ ID من رابط الصفحة `useParams()` وتجلب بيانات هذا الطبيب بالكامل عبر `fetchDoctorById(id)`.
* تعرض نبذة عن الطبيب، مؤهله العلمي، عيادته، أوقات عمله الأسبوعية، وسعر الكشف مع زر للحجز المباشر.

#### 📄 `BookAppointmentPage.jsx` (صفحة حجز الموعد `/book`)
* **الوظيفة:** استمارة حجز موعد جديد.
* **التقنيات المستخدمة فيها:**
  * **React Hook Form:** إدارة الحقول والتحقق منها (الاسم مطلوب، الإيميل صحيح، الهاتف صحيح، والتاريخ من اليوم فما فوق).
  * **Uncontrolled useRef Example:** حقل كود الخصم المرجعي `referralCodeRef` باستخدام `useRef`.
  * **Submit (POST):** عند الإرسال يتم إنشاء الموعد باسم `Jana Sayed` وإضافته عبر السيرفر وتنبيه المريض بنجاح الحجز ثم التوجيه لصفحة المواعيد.

#### 📄 `AppointmentsPage.jsx` (صفحة مواعيدي - إدارة الـ CRUD بالكامل)
* **الوظيفة:** الصفحة الخاصة بإدارة عمليات الـ CRUD الأربعة للمواعيد:
  * **Create (إنشاء):** متاح من صفحة الحجز.
  * **Read (قراءة):** يجلب المواعيد المحجوزة عبر السيرفر ويعرضها كبطاقات.
  * **Update (تحديث):** يفتح نافذة Modal لتغيير تاريخ الموعد أو وقته ويرسل طلب `PUT`.
  * **Delete (حذف):** يفتح نافذة Modal لتأكيد الإلغاء ويرسل طلب `DELETE`.

#### 📄 `ProfilePage.jsx` (صفحة البروفايل `/profile`)
* **الوظيفة:** تعتمد على Zustand لـ عرض وتعديل بياناتك الشخصية (`Jana Sayed`).
* تمكنك من تغيير بياناتك وتنسيق وضع الشاشة (Dark / Light Mode).

#### 📄 `NotFoundPage.jsx` (صفحة 404)
* تظهر تلقائياً إذا حاول المستخدم كتابة رابط غير موجود، مع زر للعودة للرئيسية.

---

## 🌍 4. خطوات الـ Deployment (رفع المشروع أونلاين على Vercel أو Netlify)

تم إعداد المشروع وتهيئة ملفات الـ SPA Routing مثل `vercel.json` و `public/_redirects` ليكون جاهزاً 100% للرفع أونلاين بضغطة زر.

### الطريقة الأولى: الرفع المباشر عبر Netlify Drop (بدون كود أو GitHub)
1. افتحي التيرمينال في مجلد المشروع واكتبي:
   ```bash
   npm run build
   ```
2. سينتج مجلد جديد اسمه **`dist`** داخل المشروع.
3. افتحي موقع **[app.netlify.com/drop](https://app.netlify.com/drop)** في المتصفح.
4. اسحبي مجلد **`dist`** وأفلتيه في الموقع مباشرة!
5. سيعطيكِ الموقع رابطاً شغالاً للمشروع أونلاين فوراً (مثل: `https://medical-booking-jana.netlify.app`).

### الطريقة الثانية: الرفع عن طريق GitHub + Vercel
1. ارفعي المجلد على حسابكِ في **GitHub**.
2. ادخلي على موقع **[Vercel.com](https://vercel.com)** واضغطي **Import Project**.
3. اختاري المستودع (Repository) واضغطي **Deploy**.
4. بفضل وجود ملف `vercel.json` في كود المشروع، سيعمل الرابط والملاحة بين الصفحات بامتياز دون أي خطأ 404!

---

## 🎯 5. كيف تشرحين المشروع للمحاضر أو المُقيّم؟ (Presentation Guide)

1. **س: هل البيانات تعتمد على الـ REST API فقط أم هناك fallback داخل الكود؟**
   * **ج:** البيانات تعتمد 100% على الـ REST API الصريح باستخدام Axios متصل بـ `json-server` عبر ملف `db.json` دون وجود أي بيانات ثابتة داخل مكونات الواجهة أو ملف الـ API.

2. **س: أين قمتِ بتطبيق الـ Controlled والـ Uncontrolled Inputs؟**
   * **ج:**
     * **Controlled Input:** في مربع البحث عن الأطباء بصفحة `DoctorsPage.jsx` حيث ترتبط قيمة المدخل بـ `useState`.
     * **Uncontrolled Input:** في حقل كود الخصم بصفحة `BookAppointmentPage.jsx` وفي المكون الإيضاحي `ControlledVsUncontrolledDemo.jsx` حيث استخدمت `useRef` لقراءة وتمرير القيمة مباشرة من الـ DOM node.

3. **س: كيف قمتِ بإنشاء إدارة المواعيد (CRUD Operations)؟**
   * **ج:** في صفحة `AppointmentsPage.jsx` ومع ملف `api.js`:
     * **Create:** عبر نموذج `BookAppointmentPage.jsx` باستخدام React Hook Form وطلب `POST`.
     * **Read:** بجلب المواعيد عبر Axios بطلب `GET`.
     * **Update:** عبر نافذة Modal وتحديث البيانات بطلب `PUT`.
     * **Delete:** بزر الإلغاء وتأكيد الحذف بطلب `DELETE`.
